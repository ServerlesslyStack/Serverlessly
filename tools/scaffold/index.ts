/**
 * This scaffolding tool is meant to be used internally at the official repo.
 * End-user scaffolding tool can be found in `packages/serverlessly` directory.
 */

import main from './src/main';

main();
